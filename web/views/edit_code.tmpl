<h1>Edit</h1>
<form action="/{{name}}/{{url}}/edit/{{line_id}}" method="get">
    <textarea name="cmd" rows="5" cols="80">{{old[0]}}</textarea>
    <br/>
    <input type="submit" name="save" value="save">
</form>
<p><small>Welcome user: {{uid}}</small></p>
%rebase base.tmpl title='Suricate - Edit'